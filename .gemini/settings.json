{
  "tools": {
    "shell": {
      "allowCommands": ["npm run lint", "npm run", "npm run test:ci", "npx playwright test", "npm run test -- --run"],
      "deniedCommands": ["npm run test:watch"]
    }
  }
}
